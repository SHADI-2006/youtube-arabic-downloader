# Personal downloaded content — never publish this
downloads/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/

# Editors — launch.json is portable and shared; settings.json holds a
# machine-specific interpreter path, so it stays local
.vscode/*
!.vscode/launch.json
.claude/

# OS
Thumbs.db
.DS_Store

# Secrets — never commit these (use environment variables instead)
.env
*.env
*cookies*.txt
